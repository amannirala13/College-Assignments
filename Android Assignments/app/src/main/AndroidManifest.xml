<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.amannirala13.mobilesiu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".assignments.asg2.activities.RestaurantEntryForm"
            android:theme="@style/Asg2Theme"/>
        <activity
            android:name=".assignments.asg2.activities.Asg2MainActivity"
            android:theme="@style/Asg2Theme" />
        <activity android:name=".assignments.asg1.Asg1Second" />
        <activity android:name=".assignments.asg1.Asg1First" />
        <activity android:name=".main.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>